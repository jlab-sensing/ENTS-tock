# Sensors App

The sensors app handles measurements from connected sensors and transferring the data to the [core](../../core) app where it gets remotely transmitted. The transfer of data between apps is handled through IPC.

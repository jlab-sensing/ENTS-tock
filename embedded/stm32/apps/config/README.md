# Configuration App

This is a standalone application for configuration. It starts a webserver that provides a user interface for saving a configuration.

Additionally it prints the current configuration to serial output.

